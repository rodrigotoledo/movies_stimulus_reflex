<div id="<%= dom_id movie %>">
  <p>
    <strong>Title:</strong>
    <%= movie.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= movie.description %>
  </p>

  <p><%=link_to "Edit", edit_movie_path(movie) %> | <%=link_to "Destroy", movie_path(movie), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %> </p>

</div>
